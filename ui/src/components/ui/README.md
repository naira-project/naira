## Shadcn Components

This directory is the default directory for `shadcn` components.  When they are generated through `npx shadcn add <component-name>`, they will be added here. Since these files are generated, they are not to be reviewed.If the default directory for `shadcn` components is to be changed, it can be done from under `components.json` > `aliases` > `ui`.