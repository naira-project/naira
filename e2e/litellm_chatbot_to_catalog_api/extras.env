# Scenario-only test fixtures that aren't in either Helm chart (they're not
# shared components or naira-core pieces) — applied directly as manifests by
# create-environment.sh. Space-separated names of dirs under e2e/components/.
EXTRAS="chatbot1"
