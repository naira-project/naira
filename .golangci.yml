version: "2"

linters:
  default: none
  enable:
    - gocritic
    - revive
    - nolintlint
  settings:
    gocritic:
      enabled-checks:
        - ruleguard
      settings:
        ruleguard:
          rules: "rules/ruleguard.go"
    revive:
      rules:
        - name: exported
          severity: warning
    nolintlint:
      require-explanation: true
      require-specific: true
  exclusions:
    rules:
      - linters:
          - revive
        text: "should have comment"
