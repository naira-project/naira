# Slim OpenMetadata server values for local kind development
pipelineServiceClientEnabled: false

openmetadata:
  config:
    pipelineServiceClientConfig:
      enabled: false

resources:
  requests:
    cpu: 500m
    memory: 1Gi
  limits:
    cpu: 2
    memory: 2Gi
