## Authentication through Keycloak in Dev Environment

Please go to `http://localhost:8080` and go to `Manage realms > naira > Client scopes > roles (on the next page) > Mappers > realm roles > Toggle "Add to ID token"`. 