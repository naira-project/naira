# Smallest values that render: names the Secrets an operator supplies.
# The chart refuses to render without a Secret source (templates/validate.yaml).
catalog:
  secret:
    existingSecret: catalog-secrets
portal:
  oidc:
    secret:
      existingSecret: portal-oidc
