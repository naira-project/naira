apiVersion: v1
kind: Service
metadata:
  name: {{ include "ui-poc.fullname" . }}
  labels:
    {{- include "ui-poc.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ui-poc.selectorLabels" . | nindent 4 }}
